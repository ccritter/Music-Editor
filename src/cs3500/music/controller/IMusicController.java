package cs3500.music.controller;

/**
 * Interface for a controller for a music editor, which allows the user to control and manipulate
 * the music model and it's corresponding view.
 */
public interface IMusicController {

  /**
   * Method which initializes the model and displays it's chosen view.
   */
  void display();

}
